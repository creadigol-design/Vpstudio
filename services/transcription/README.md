# transcription (planned — Phase 2)

Speech-to-text for English, Welsh and mixed-language recordings with word- and
segment-level timestamps (spec §19). Pluggable providers — Welsh quality may
require provider fallback (spec §45.4). Output feeds caption generation and
script alignment. Transcripts are stored on `Transcript`; editable caption
cues on `CaptionTrack`.
